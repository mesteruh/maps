Ок, делаем так, чтобы **сразу озвучивалось по умолчанию**. Но есть 2 реальности, которые нельзя “закодить” в обход:

1. **Браузерный TTS (`speechSynthesis`) на Linux работает только если в системе/песочнице реально доступны голоса.** Иногда `getVoices()` пустой, особенно в некоторых сборках/песочницах. ([Stack Overflow][1])
2. Во многих браузерах **любая “авто-озвучка” может требовать первого жеста пользователя** (клик/тап), иначе её блокируют как autoplay. Поэтому лучший UX для ребёнка: **большая кнопка “▶ Начать”** (иконка), один тап — и дальше всё озвучивается автоматически.

Ниже — markdown для Codex уже **без фильтров**, с **автоозвучкой по умолчанию**, и с “unlock” через один тап.

````markdown
# Одностраничник “Страны мира” (флаг + страна + столица + автоозвучка)

Сделай простой статический одностраничник: ребёнок листает карточки стран, и каждая карточка озвучивается автоматически (ребёнок не читает). Без фильтров. Без фреймворков. Хостинг любой.

## Важное про озвучку (Linux)
Озвучка делается браузером через Web Speech API: `window.speechSynthesis`. Это НЕ “виндовый TTS”, это браузерный API. На Linux будет говорить, если у браузера есть доступные голоса. Иногда в песочницах (snap/flatpak) голоса/ speech-dispatcher могут быть недоступны и `getVoices()` пустой. :contentReference[oaicite:1]{index=1}

Чтобы “по дефолту озвучка шла” и не упиралась в autoplay-политику, сделай первый экран с огромной кнопкой:
- “▶ НАЧАТЬ” (без текста можно, просто большая кнопка с иконкой).
- Один тап/клик — и дальше автоозвучка включена всегда.

Если голосов нет — покажи крупное сообщение для взрослого: “Озвучка недоступна: нет голосов в браузере/системе”.

## 1) Файлы
Сгенерируй полностью:
- `index.html`
- `styles.css`
- `app.js`

## 2) Данные о странах
Используй REST Countries:
`fetch("https://restcountries.com/v3.1/all")`

Нормализуй к объекту:
```js
{
  code: "RU",
  nameRu: "Россия" | "",      // translations.rus.common если есть
  nameEn: "Russia",           // name.common
  capital: "Moscow" | "",     // capital?.[0] || ""
  flagSvg: "https://...",     // flags.svg
  flagPng: "https://..."      // flags.png
}
````

Показ:

* Заголовок страны: `nameRu` если есть, иначе `nameEn`
* Столица: если пусто — “Столица неизвестна”

## 3) UI

Экран состоит из:

* Overlay “Старт” (полупрозрачный слой) + огромная кнопка ▶.
* Карточка по центру: флаг, страна, столица.
* Кнопки: “←”, “→”, “🎲” (случайная)
* Прогресс `N / M`

Горячие клавиши:

* `←` / `→` — листать
* `R` — случайная
* (Space не обязателен, т.к. автоозвучка)

## 4) Автоозвучка по умолчанию

* `state.autoplay = true` по дефолту.
* После каждого `render()` вызывай `speakCurrent()` (но только после первого “unlock” кликом по старт-кнопке).
* Перед каждой озвучкой делай `speechSynthesis.cancel()` чтобы не наслаивалось.

Фраза озвучки:

* Если столица есть: `Страна: {страна}. Столица: {столица}.`
* Если нет: `Страна: {страна}. Столица неизвестна.`

## 5) Голоса / выбор русского

Логика:

* дождаться, пока `speechSynthesis.getVoices()` станет непустым (событие `onvoiceschanged` + ретрай таймером).
* выбрать голос, где `voice.lang` начинается с `ru`
* если нет русского — использовать первый доступный

Если голосов нет вообще:

* показать предупреждение в UI (для взрослого), но приложение продолжает листать карточки.

## 6) app.js — обязательные функции

Сделай простой state:

```js
const state = {
  countries: [],
  index: 0,
  autoplay: true,   // ВКЛ по умолчанию
  unlocked: false,  // станет true после нажатия “▶”
  voice: null,
  voicesReady: false
};
```

Функции:

* `init()`
* `loadCountries()`
* `normalizeCountry(c)`
* `render()`
* `next()`, `prev()`, `random()`
* `initVoices()`, `pickVoice()`
* `speak(text)`, `speakCurrent()`

## 7) index.html требования

* Overlay со старт-кнопкой (крупная, по центру).
* Контейнер карточки (img флага, страна, столица, прогресс).
* Кнопки навигации (крупные).
* Небольшая область “статус/ошибка” (если нет голосов/нет сети).

## 8) Acceptance criteria

* Открыл страницу → вижу кнопку ▶ → нажал → сразу зазвучала первая страна.
* Листаю → каждая новая карточка озвучивается автоматически.
* Никаких наложений речи.
* Если сеть не доступна — понятное сообщение.
* Если голосов нет — понятное сообщение, но UI работает.

---

Сгенерируй полностью `index.html`, `styles.css`, `app.js`.

```

### Что нужно учесть у тебя “на линуксе”, чтобы это реально **говорило**
Это важно: сайт будет говорить **на устройстве ребёнка** (клиент), не на твоём сервере.

Если окажется, что в браузере голоса пустые/не работают:
- на Linux часто помогает наличие **speech-dispatcher + espeak/espeak-ng** (как системная TTS-база). :contentReference[oaicite:2]{index=2}  
- в некоторых сборках Chromium озвучка включается флагом `--enable-speech-dispatcher`. :contentReference[oaicite:3]{index=3}  
- в sandbox-упаковках (flatpak/snap) бывают проблемы с доступом к speech-dispatcher. :contentReference[oaicite:4]{index=4}  

Если хочешь “вообще без сюрпризов”, то 100% гарантия — **не браузерный TTS**, а **готовые аудиофайлы** (mp3/ogg) на твоём сервере (генеришь один раз, дальше `<audio autoplay>`). Но это уже другой вариант (я могу дать markdown и под него тоже).
::contentReference[oaicite:5]{index=5}
```

[1]: https://stackoverflow.com/questions/46863170/speechsynthesis-getvoices-is-empty-array-in-chromium-fedora?utm_source=chatgpt.com "speechSynthesis.getVoices() is empty array in Chromium ..."
